@inject NavigationManager Navigation
@inject IJSRuntime JS
<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" @onclick="GoHome">
            Skolaris
        </a>
    </div>
</div>
<input type="checkbox" title="Navigation menu" class="navbar-toggler" />
<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="flex-column">
        <!-- HOME -->
        <div class="nav-item px-3">
            <a class="nav-link" @onclick="GoHome">
                <span class="bi bi-house-door-fill-nav-menu"></span>
                Home
            </a>
        </div>
        <!-- ADMIN SEULEMENT -->
        @if (role == "ADMIN")
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="users-management">
                    <span class="bi bi-people-fill"></span>
                    Gérer les utilisateurs
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="admin/cours">
                    <span class="bi bi-book-fill"></span>
                    Cours
                </NavLink>
            </div>
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="admin/emploi-du-temps">
                    <span class="bi bi-calendar-fill"></span>
                    Emploi du temps
                </NavLink>
            </div>
        }
        <!-- ENSEIGNANT SEULEMENT -->
        @if (role == "ENSEIGNANT")
        {
            <div class="nav-item px-3">
                <NavLink class="nav-link" href="enseignant/emploi-du-temps">
                    <span class="bi bi-calendar-fill"></span>
                    Mon emploi du temps
                </NavLink>
            </div>
        }
        <!-- ELEVE SEULEMENT -->
        @if (role == "ELEVE")
        {
            <div class="nav-it